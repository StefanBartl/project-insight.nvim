-- project_insight.nvim — plugin entry point
-- The plugin is lazy-loaded; setup() must be called by the user.
-- This file exists so that :checkhealth project-insight works without setup().
if vim.g.loaded_project_insight then return end
vim.g.loaded_project_insight = true
